"""INT8 quantization for on-device inference (Android keyboard stretch goal).

Target: mT5-small + LoRA adapter quantised to ~80 MB for on-device use.
Tool: HuggingFace Optimum

    pip install optimum
"""
